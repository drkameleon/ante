weight: 3
title: "Integrated Build System"
description: >
    <div class="highlight-textalign-left"><pre><span class="c1">//build system is implemented as a library

    //with no syntax changes to the language itself</span>

    <span class="k">import</span> <span class="kt">Build</span>, <span class="kt">File</span>


    default_target <span class="s">"program"</span>


    target <span class="s">"program"</span> =
        depend_on <span class="s">"legacy_cpp"</span>
        compile <span class="s">"driver.an"</span>

    target <span class="s">"legacy_cpp"</span> =
        <span class="k">let</span> cppfiles = glob <span class="s">"*.cpp"</span>
        map cxx cppfiles
        link_with cppfiles</span></code></pre></div>
