baseURL = "https://jfecher.github.io/ante/public/"
title = "Ante"

theme = "hugo-universal-theme"

languageCode = "en-us"
defaultContentLanguage = "en"

pygmentsUseClassic = true
pygmentsUseClasses = true
pygmentsCodeFences = true

newContentEditor = "vim"

[params]
  description = "A systems programming language focused on providing extreme extensibility without compromising on speed or readability"
  author = "jfecher"
  defaultKeywords = ["ante", "programming", "language", "compiler"]
  style = "red"
  date_format = ""


[menu]

[[menu.main]]
    name = "Home"
    url = "/"
    weight = 1

[[menu.main]]
    name = "Docs"
    url = "/tutorial/"
    weight = 2


[[menu.topbar]]
    weight = 1
    name = "GitHub"
    url = "https://github.com/jfecher/ante"
    pre = "<i class='fa fa-2x fa-github'></i>"
    logo = "img/ante-lowres.png"

# Enable or disable top bar with social icons
[params.topbar]
    enable = false
    text = """<p class="hidden-sm hidden-xs">Contact us on +420 777 555 333 or hello@universal.com.</p>
      <p class="hidden-md hidden-lg"><a href="#" data-animate-hover="pulse"><i class="fa fa-phone"></i></a>
      <a href="#" data-animate-hover="pulse"><i class="fa fa-envelope"></i></a>
      </p>
      """

# Enable and disable widgets for the right sidebar
[params.widgets]
    categories = true
    tags = true
    search = false

[params.carousel]
    enable = true
    # All carousel items are defined in their own files. You can find example items
    # at 'exampleSite/data/carousel'.
    # For more informtion take a look at the README.

[params.features]
    enable = true
    # All features are defined in their own files. You can find example items
    # at 'exampleSite/data/features'.
    # For more informtion take a look at the README.

[params.testimonials]
    enable = false
    # All testimonials are defined in their own files. You can find example items
    # at 'exampleSite/data/testimonials'.
    # For more informtion take a look at the README.
    title = "Testimonials"
    subtitle = "We have worked with many clients and we always like to hear they come out from the cooperation happy and satisfied. Have a look what our clients said about us."

[params.see_more]
    enable = true
    icon = "fa fa-github"
    title = "The compiler is on GitHub"
    subtitle = "All are welcome to contribute!"
    link_url = "https://github.com/jfecher/ante"
    link_text = "github.com/jfecher/ante"

[params.clients]
    enable = false
    # All clients are defined in their own files. You can find example items
    # at 'exampleSite/data/clients'.
    # For more informtion take a look at the README.
    title = "Our Clients"
    subtitle = ""

[params.recent_posts]
    enable = false
    title = "From our Latin blog, apparently."
    subtitle = "Why are you still reading?"
